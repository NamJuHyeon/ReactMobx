import { observer, action, computed } from "mobx";

export default class megaStore {
  // =================================================observable=================================================
  // @observer
  // ==================================================computed==================================================
  // @computed
  // ===================================================action===================================================
  // @action
}
